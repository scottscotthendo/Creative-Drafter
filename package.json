{
  "name": "heidi-creative-studio",
  "version": "0.1.0",
  "private": true,
  "description": "AI-assisted creative drafting for Heidi Health",
  "workspaces": [
    "packages/core",
    "packages/web",
    "packages/cli"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/core && npm run build --workspace=packages/web && npm run build --workspace=packages/cli",
    "cli": "npm run start --workspace=packages/cli"
  },
  "engines": {
    "node": ">=18"
  }
}
